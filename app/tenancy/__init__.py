"""Multi-tenant commercial layer (opt-in via COPILOT_MULTI_TENANT).

Adds organizations, members + RBAC, tenant-scoped API keys, per-tenant encrypted
integration secrets, and usage metering — the foundation for selling DevOps-Copilot
as a B2B SaaS. Entirely additive: when multi-tenancy is off the app runs exactly
as the single-tenant offline demo.
"""
